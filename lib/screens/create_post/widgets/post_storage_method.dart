part of '../post_info_screen.dart';

class PostStorageMethod extends StatelessWidget {
  const PostStorageMethod({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
