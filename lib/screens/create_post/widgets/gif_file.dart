part of '../create_post_screen.dart';
class GetGif extends StatelessWidget {
  const GetGif({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
